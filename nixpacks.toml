[phases.setup]
nixPkgs = ["nodejs_20", "python312", "tesseract", "poppler_utils"]

[phases.install]
cmds = ["npm ci", "python3 -m pip install pymupdf pdfplumber pdf2image pytesseract --quiet --break-system-packages"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
