[phases.setup]
nixPkgs = ["nodejs_20", "python312", "tesseract", "poppler_utils"]

[phases.install]
cmds = ["npm ci", "pip install pdfplumber pdf2image pytesseract --quiet"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
